<x-app-layout>
    <x-slot name="header"><div class="flex items-end justify-between"><div><p class="text-sm font-semibold uppercase tracking-[0.2em] text-cyan-700">Staff</p><h2 class="text-2xl font-bold text-slate-900">{{ $staffMember->name }}</h2></div><a href="{{ route('staff.edit', $staffMember) }}" class="rounded-full bg-slate-900 px-4 py-2 text-sm font-semibold text-white">Modifier</a></div></x-slot>
    <div class="grid gap-6 xl:grid-cols-3"><section class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm xl:col-span-2"><div class="grid gap-4 md:grid-cols-2"><div class="rounded-2xl bg-slate-50 p-4"><div class="text-sm text-slate-500">Rôle</div><div class="mt-1 font-semibold text-slate-900">{{ $staffMember->roleLabel }}</div></div><div class="rounded-2xl bg-slate-50 p-4"><div class="text-sm text-slate-500">Spécialité</div><div class="mt-1 font-semibold text-slate-900">{{ $staffMember->specialty ?? 'N/A' }}</div></div><div class="rounded-2xl bg-slate-50 p-4"><div class="text-sm text-slate-500">Téléphone</div><div class="mt-1 font-semibold text-slate-900">{{ $staffMember->phone ?? 'N/A' }}</div></div><div class="rounded-2xl bg-slate-50 p-4"><div class="text-sm text-slate-500">Email</div><div class="mt-1 font-semibold text-slate-900">{{ $staffMember->email }}</div></div></div></section><aside class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><h3 class="text-lg font-bold text-slate-900">Classes affectées</h3><div class="mt-4 space-y-3">@forelse($staffMember->schoolClasses as $class)<div class="rounded-2xl bg-slate-50 p-4"><div class="font-semibold text-slate-900">{{ $class->name }}</div><div class="text-sm text-slate-600">{{ $class->students->count() }} élèves</div></div>@empty<div class="text-sm text-slate-500">Aucune classe attribuée.</div>@endforelse</div></aside></div>
</x-app-layout>
