<x-app-layout>
    <x-slot name="header"><div class="flex items-end justify-between"><div><p class="text-sm font-semibold uppercase tracking-[0.2em] text-cyan-700">Message</p><h2 class="text-2xl font-bold text-slate-900">{{ $message->subject }}</h2></div><a href="{{ route('messages.create') }}" class="rounded-full bg-slate-900 px-4 py-2 text-sm font-semibold text-white">Nouveau message</a></div></x-slot>
    <div class="grid gap-6 xl:grid-cols-3"><section class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm xl:col-span-2"><div class="flex items-center justify-between border-b border-slate-200 pb-4"><div><div class="text-sm text-slate-500">De</div><div class="mt-1 font-semibold text-slate-900">{{ $message->sender?->name }}</div></div><div><div class="text-sm text-slate-500">À</div><div class="mt-1 font-semibold text-slate-900">{{ $message->recipient?->name }}</div></div></div><div class="mt-6 rounded-2xl bg-slate-50 p-5"><p class="whitespace-pre-line text-sm leading-7 text-slate-700">{{ $message->body }}</p></div></section><aside class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div class="space-y-3 text-sm"><div class="rounded-2xl bg-slate-50 p-4">Statut: {{ $message->isRead() ? 'Lu' : 'Non lu' }}</div><div class="rounded-2xl bg-slate-50 p-4">Créé le: {{ $message->created_at->format('d/m/Y H:i') }}</div><div class="flex flex-col gap-3"><form method="POST" action="{{ route('messages.read', $message) }}">@csrf @method('PATCH')<button class="rounded-full bg-cyan-400 px-4 py-2 text-sm font-semibold text-slate-950">Marquer comme lu</button></form><form method="POST" action="{{ route('messages.destroy', $message) }}" onsubmit="return confirm('Supprimer ce message ?')">@csrf @method('DELETE')<button class="rounded-full border border-rose-300 px-4 py-2 text-sm font-semibold text-rose-700">Supprimer</button></form></div></div></aside></div>
</x-app-layout>
