<x-app-layout>
    <x-slot name="header"><div class="flex items-end justify-between"><div><p class="text-sm font-semibold uppercase tracking-[0.2em] text-cyan-700">Messages</p><h2 class="text-2xl font-bold text-slate-900">Messages envoyés</h2></div><a href="{{ route('messages.create') }}" class="rounded-full bg-slate-900 px-4 py-2 text-sm font-semibold text-white">Nouveau message</a></div></x-slot>
    <div class="overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-slate-600"><tr><th class="px-4 py-3 text-left">À</th><th class="px-4 py-3 text-left">Sujet</th><th class="px-4 py-3 text-left">Statut</th><th class="px-4 py-3 text-left">Actions</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($messages as $message)<tr><td class="px-4 py-4 font-semibold text-slate-900">{{ $message->recipient?->name }}</td><td class="px-4 py-4 text-slate-600">{{ $message->subject }}</td><td class="px-4 py-4"><span class="rounded-full px-3 py-1 text-xs font-semibold bg-slate-100 text-slate-700">{{ $message->isRead() ? 'Lu' : 'Non lu' }}</span></td><td class="px-4 py-4"><div class="flex flex-wrap gap-2"><a href="{{ route('messages.show', $message) }}" class="rounded-full border border-slate-300 px-3 py-1 text-xs font-semibold text-slate-700">Ouvrir</a><form method="POST" action="{{ route('messages.destroy', $message) }}" onsubmit="return confirm('Supprimer ce message ?')">@csrf @method('DELETE')<button class="rounded-full border border-rose-300 px-3 py-1 text-xs font-semibold text-rose-700">Supprimer</button></form></div></td></tr>@empty<tr><td colspan="4" class="px-4 py-8 text-center text-slate-500">Aucun message envoyé.</td></tr>@endforelse</tbody></table><div class="border-t border-slate-200 bg-slate-50 px-4 py-4">{{ $messages->links() }}</div></div>
</x-app-layout>
