<x-app-layout>
    <x-slot name="header"><div><p class="text-sm font-semibold uppercase tracking-[0.2em] text-cyan-700">Note</p><h2 class="text-2xl font-bold text-slate-900">{{ $grade->student?->full_name }}</h2></div></x-slot>
    <div class="grid gap-6 xl:grid-cols-3"><section class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm xl:col-span-2"><div class="grid gap-4 md:grid-cols-2"><div class="rounded-2xl bg-slate-50 p-4"><div class="text-sm text-slate-500">Matière</div><div class="mt-1 font-semibold text-slate-900">{{ $grade->subject?->name }}</div></div><div class="rounded-2xl bg-slate-50 p-4"><div class="text-sm text-slate-500">Note</div><div class="mt-1 font-semibold text-slate-900">{{ number_format((float) $grade->score, 2) }}</div></div><div class="rounded-2xl bg-slate-50 p-4"><div class="text-sm text-slate-500">Coefficient</div><div class="mt-1 font-semibold text-slate-900">{{ number_format((float) $grade->coefficient, 2) }}</div></div><div class="rounded-2xl bg-slate-50 p-4"><div class="text-sm text-slate-500">Semestre</div><div class="mt-1 font-semibold text-slate-900">{{ $grade->semester }}</div></div></div><p class="mt-6 text-sm leading-7 text-slate-600">{{ $grade->observation ?: 'Aucune observation.' }}</p></section><aside class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><h3 class="text-lg font-bold text-slate-900">Contexte</h3><div class="mt-4 space-y-3 text-sm"><div class="rounded-2xl bg-slate-50 p-4">Classe: {{ $grade->student?->schoolClass?->name ?? 'N/A' }}</div><div class="rounded-2xl bg-slate-50 p-4">Enseignant: {{ $grade->teacher?->name ?? 'N/A' }}</div><div class="rounded-2xl bg-cyan-50 p-4 text-cyan-900">Bulletin: <a href="{{ route('students.bulletin', $grade->student) }}" class="font-semibold underline">ouvrir</a></div></div></aside></div>
</x-app-layout>
