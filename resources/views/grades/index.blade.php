<x-app-layout>
    <x-slot name="header"><div class="flex items-end justify-between"><div><p class="text-sm font-semibold uppercase tracking-[0.2em] text-cyan-700">Évaluation</p><h2 class="text-2xl font-bold text-slate-900">Notes</h2></div><a href="{{ route('grades.create') }}" class="rounded-full bg-slate-900 px-4 py-2 text-sm font-semibold text-white">Nouvelle note</a></div></x-slot>
    <div class="space-y-6"><section class="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><form method="GET" class="grid gap-4 md:grid-cols-4"><select name="class_id" class="rounded-xl border-slate-300 px-4 py-3 text-sm"><option value="">Toutes les classes</option>@foreach($classes as $class)<option value="{{ $class->id }}" @selected((string) request('class_id') === (string) $class->id)>{{ $class->name }}</option>@endforeach</select><select name="student_id" class="rounded-xl border-slate-300 px-4 py-3 text-sm"><option value="">Tous les élèves</option>@foreach($students as $student)<option value="{{ $student->id }}" @selected((string) request('student_id') === (string) $student->id)>{{ $student->full_name }}</option>@endforeach</select><select name="subject_id" class="rounded-xl border-slate-300 px-4 py-3 text-sm"><option value="">Toutes les matières</option>@foreach($subjects as $subject)<option value="{{ $subject->id }}" @selected((string) request('subject_id') === (string) $subject->id)>{{ $subject->name }}</option>@endforeach</select><div class="flex gap-3"><input name="semester" value="{{ request('semester') }}" placeholder="Semestre" class="rounded-xl border-slate-300 px-4 py-3 text-sm"><button class="rounded-xl bg-cyan-400 px-4 py-3 text-sm font-semibold text-slate-950">Filtrer</button></div></form></section><section class="overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-slate-600"><tr><th class="px-4 py-3 text-left">Élève</th><th class="px-4 py-3 text-left">Matière</th><th class="px-4 py-3 text-left">Note</th><th class="px-4 py-3 text-left">Coef.</th><th class="px-4 py-3 text-left">Semestre</th><th class="px-4 py-3 text-left">Actions</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($grades as $grade)<tr><td class="px-4 py-4 font-semibold text-slate-900">{{ $grade->student?->full_name }}</td><td class="px-4 py-4 text-slate-600">{{ $grade->subject?->name }}</td><td class="px-4 py-4 font-semibold text-slate-900">{{ number_format((float) $grade->score, 2) }}</td><td class="px-4 py-4">{{ number_format((float) $grade->coefficient, 2) }}</td><td class="px-4 py-4">{{ $grade->semester }}</td><td class="px-4 py-4"><div class="flex flex-wrap gap-2"><a href="{{ route('grades.show', $grade) }}" class="rounded-full border border-slate-300 px-3 py-1 text-xs font-semibold text-slate-700">Voir</a><a href="{{ route('grades.edit', $grade) }}" class="rounded-full border border-slate-300 px-3 py-1 text-xs font-semibold text-slate-700">Modifier</a><form method="POST" action="{{ route('grades.destroy', $grade) }}" onsubmit="return confirm('Supprimer cette note ?')">@csrf @method('DELETE')<button class="rounded-full border border-rose-300 px-3 py-1 text-xs font-semibold text-rose-700">Supprimer</button></form></div></td></tr>@empty<tr><td colspan="6" class="px-4 py-8 text-center text-slate-500">Aucune note trouvée.</td></tr>@endforelse</tbody></table><div class="border-t border-slate-200 bg-slate-50 px-4 py-4">{{ $grades->links() }}</div></section></div>
</x-app-layout>
